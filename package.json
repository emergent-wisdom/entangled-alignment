{
  "name": "entangled-alignment-toolchain",
  "version": "0.0.0",
  "private": true,
  "description": "Pinned toolchain for the Entangled Alignment reading runs: the understanding-graph MCP server and viewer, plus the embedding backend the swarm agents depend on.",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@xenova/transformers": "2.17.2",
    "understanding-graph": "0.1.16"
  },
  "overrides": {
    "better-sqlite3": "13.0.3"
  }
}
